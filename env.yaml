apiVersion: v1
kind: Pod
metadata:
  name: env-demo
spec:
  containers:
  - name: nginx
    image: nginx
    env:
    - name: frontend
      value: nginx-service
    - name: nginx-port
      value: "80"
    - name: ENV
      value: prod